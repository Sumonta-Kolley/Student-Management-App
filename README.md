## How to Run
1. Compile the code using GCC: `gcc student_management.c -o student_app`
2. Run the application: `./student_app` (or `student_app.exe` on Windows)

**Note:** The program will automatically create a `student_data.bin` file in the same folder to save your records permanently.
